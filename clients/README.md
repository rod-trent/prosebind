# Clients

Thin adapters. All narrative logic lives in the engine — a client that grows its own
continuity rules is a bug.

- `vscode/` — LSP client
- `obsidian/` — plugin wrapping the same server

Word and Google Docs adapters are v2. See `DESIGN.md` § 5 and § 11.
